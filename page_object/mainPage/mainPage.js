const editorTabMenu = require("./editorTabMenu")
const previewPan = require("./previewPan")
const popOver = require("../elements/popOver")

module.exports = {
    url: "https://webapp.wisestamp.com/",
    sections: {popOver, editorTabMenu, previewPan}
}